// Example file demonstrating ESLint compliance
// All linting violations from the previous commit have been fixed

function greet(name) {
    console.log('Hello, ' + name);
    return 'Greeting sent';
}

function calculateSum(a, b) {
    const result = a + b;
    return result;
}

greet('World');
calculateSum(5, 10);
